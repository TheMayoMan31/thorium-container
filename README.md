# Thorium Isolated

A containerised Thorium browser via Podman with persistent profile and Startpage as default search engine.

## Build
```bash
podman build -t thorium-isolated .
```

## Run
See `launch-thorium.sh` for the full run command with Wayland forwarding.
